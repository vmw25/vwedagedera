build:
  # Keep this aligned with the local, Codespaces, and GitHub Actions builds.
  hugo_version: '0.161.1'
deploy:
  # Deployment target: github-pages, netlify, vercel, cloudflare, or none
  host: 'github-pages'
template:
  id: "dev-portfolio"
  name: "Developer Portfolio"
