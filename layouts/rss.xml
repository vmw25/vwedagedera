{{- /* Preserve the HugoBlox RSS output, but keep article emails to a short preview. */ -}}
{{- $pageContext := cond .IsHome site . -}}
{{- $pages := $pageContext.RegularPages -}}
{{- $pages = where $pages "Params.private" "!=" true -}}
{{- $limit := site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
  {{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\" ?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if ne .Title site.Title }}{{ with .Title }}{{ . }} | {{ end }}{{ end }}{{ site.Title }}</title>
    <link>{{ .Permalink }}</link>
    {{- with .OutputFormats.Get "RSS" }}
      {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{ end -}}
    <description>{{ .Title | default site.Title }}</description>
    <generator>HugoBlox Kit (https://hugoblox.com)</generator>
    {{- with site.Language.Locale }}<language>{{ . }}</language>{{ end -}}
    {{- with site.Copyright }}<copyright>{{ strings.ReplacePairs . "{year}" now.Year "&copy;" "©" | plainify }}</copyright>{{ end -}}
    {{- if not .Date.IsZero }}<lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end -}}
    {{- if .Scratch.Get "og_image" }}
    <image>
      <url>{{ .Scratch.Get "og_image" }}</url>
      <title>{{ .Title | default site.Title }}</title>
      <link>{{ .Permalink }}</link>
    </image>
    {{ end -}}
    {{ range $pages }}
      {{- $page := . -}}
      {{- $description := $page.Content | html -}}
      {{- if eq $page.Section "articles" -}}
        {{- $firstParagraph := "" -}}
        {{- range split $page.RawContent "\n\n" -}}
          {{- $candidate := strings.TrimSpace . -}}
          {{- if and
            (eq $firstParagraph "")
            (ne $candidate "")
            (not (strings.HasPrefix $candidate "<!--"))
            (not (strings.HasPrefix $candidate "#"))
            (not (strings.HasPrefix $candidate "!"))
            (not (strings.HasPrefix $candidate "{{"))
            (not (strings.HasPrefix $candidate "<figure"))
          -}}
            {{- $firstParagraph = $page.RenderString $candidate | plainify | strings.TrimSpace -}}
          {{- end -}}
        {{- end -}}
        {{- if eq $firstParagraph "" -}}
          {{- $firstParagraph = $page.Summary | plainify | strings.TrimSpace -}}
        {{- end -}}
        {{- $description = $firstParagraph -}}
      {{- end }}
    <item>
      <title>{{ $page.Title }}</title>
      <link>{{ $page.Permalink }}</link>
      <pubDate>{{ $page.Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      <guid>{{ $page.Permalink }}</guid>
      <description>{{ $description }}</description>
    </item>
    {{ end }}
  </channel>
</rss>
